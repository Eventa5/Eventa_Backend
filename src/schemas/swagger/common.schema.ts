/**
 * 共用的 Swagger Schema 定義
 * 此檔案包含在多個 API 路由中重複使用的 schema 定義
 */

/**
 * @swagger
 * components:
 *   schemas:
 *     ErrorResponse:
 *       type: object
 *       properties:
 *         message:
 *           type: string
 *           example: 錯誤訊息
 *         status:
 *           type: boolean
 *           example: false
 */
